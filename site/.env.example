# No environment variables are required.
#
# The site builds fully static and the Keystatic editor runs in local mode
# during `npm run dev` only. If a web-based CMS is ever needed in the future,
# see docs/EDITING.md ("Future options") before adding anything here.
